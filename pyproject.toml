[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chutes-telegram-bot"
version = "1.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "python-telegram-bot>=22.0",
    "aiohttp>=3.10.0",
    "python-dotenv",
]
